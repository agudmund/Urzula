# -Urzula - requirements.in pip dependency set
# -The last of the urzula package list hummed a small tune while the games loaded, For Enjoying
# -Built using a single shared braincell by Yours Truly and various Intelligences
# No version pins; Python is the floor, pip resolves the rest.

PySide6                 # Qt bindings
pygame                  # game loop / sound
